
Install Nginix

# 1. Update your package list
sudo apt update

# 2. Install Nginx
sudo apt install nginx -y

# 3. Start Nginx
sudo systemctl start nginx

# 4. Enable Nginx to auto-start on boot
sudo systemctl enable nginx

# 5. Check status
sudo systemctl status nginx


Install CertBot
collubi-portainer
sudo apt install certbot python3-certbot-nginx


##################################################################################################################

sudo certbot

sudo nginx -t

# Fir nginx test aur reload karo
sudo nginx -t
sudo systemctl reload nginx

##################################################################################################################

http://ai-call-assistant-api.devssh.xyz/


sudo vim /etc/nginx/sites-available/ai_call_assistant_daniel
sudo ln -s /etc/nginx/sites-available/ai_call_assistant_daniel /etc/nginx/sites-enabled
sudo systemctl restart nginx


# Fir nginx test aur reload karo
sudo nginx -t
sudo systemctl reload nginx


server {
    listen 80;
    server_name ai-call-assistant-api.devssh.xyz;

     proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;


    location / {

        proxy_pass http://localhost:8010;
    }
}





server {
    listen 80;
    server_name collubichat.devssh.xyz;

     proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;


    location / {

        proxy_pass localhost:8008;
    }
}




SOCKETS

server {
    listen 80;
    server_name lawfirm-chatting.devssh.xyz;

    location / {
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header Host $host;

      proxy_pass http://localhost:9008;

      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
    }
  }





sudo certbot

sudo nginx -t


sudo vim /etc/nginx/sites-available/e_learning_api
sudo ln -s /etc/nginx/sites-available/e_learning_api /etc/nginx/sites-enabled
sudo systemctl restart nginx


server {
    listen 80;
    server_name e-learning.devssh.xyz;

     proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;


    location / {

        proxy_pass http://52.90.171.118:8004;
    }
}





FOR DELETE
1. Old config disable/remove karo
# Disable symlink
sudo rm /etc/nginx/sites-enabled/collubi_portainer

# Delete config file
sudo rm /etc/nginx/sites-available/collubi_portainer


2. Certbot ke purane certs bhi hatao (agar chahte ho bilkul clean slate)
sudo certbot delete --cert-name collubi-portainer.devssh.xyz


3. Nginx reload/check
sudo nginx -t
sudo systemctl reload nginx


